#include<stdio.h>
#include<math.h>
int main()
{
    int N,i;
    double sum=0.0;
    scanf("%d",&N);
    if(N>0){
        for(i=1;i<=N;i++){
            sum=sum+sqrt(i);
        }
    }
    printf("sum = %.2lf",sum);
    return 0;
}
