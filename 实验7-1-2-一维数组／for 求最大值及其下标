#include<iostream>
int main()
{
	using namespace std;
	int i,n,max,maxnum,a[10];
	cin>>n;
	for(i=0;i<n;i++){
		cin>>a[i];
	}
	max=a[0],maxnum=0;
	for(i=1;i<n;i++){
		if(a[i]>max){
			max=a[i];
			maxnum=i;
		}
	}
	cout<<max<<" "<<maxnum;
	return 0;
}
